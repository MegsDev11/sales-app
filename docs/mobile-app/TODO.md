# Mobile app TODO — remaining (post Phase 0–3)

Shipped in monorepo: Expo app shells + tech jobs/clock + stock scan + client credentials/layout/chat + web Jobs/Timesheets/Messages.

## Later

- [ ] Expo push notifications (job assigned, support reply, offline router)
- [ ] Offline queue for tech status / clock when no signal
- [ ] Wireless tech sketch submit from mobile (`POST /api/wireless` create_submission)
- [ ] Continuous location pings while clocked in (throttled UI indicator)
- [ ] App Store / Play listing + privacy policy (GPS + chat)
- [ ] Client account issue UI on web (API exists: `POST /api/client-accounts`)
